package com.amalitech.constants;

/** Enumeration representing the different types of bank accounts available in the system. */
public enum AccountType {
  /** Represents a savings account with interest and minimum balance requirements. */
  SAVINGS,
  /** Represents a checking account with overdraft protection and monthly fees. */
  CHECKING
}
